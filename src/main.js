/* import { aggregateCal } from './data.js';
 
let printCurious = document.getElementById("data")
window.addEventListener("load", function(event) {

  let chars = []
  for (let i = 0; i < movies.length; i++) {
  chars.push(movies[i].people.length)
  }
    const curiosidades = calcAgregado(chars, event.target.value)
    const totalCharacters = curiosidades.total

printCurious.innerHTML =
`<div class="curious">
<p class="sum">A soma de todos os personagens é: ${totalCharacters}</p>
</div>`
}); 
*/